// Lancer un service 42school/engineering-bay en 2 répliques et faire en sorte que le service fonctionne
// (se référer à la doc fournie dans hub.docker.com). Ce service s’appellera engineering-bay et sera sur le
// reseau overmind

docker service create --replicas 2 --network overmind -e OC_USERNAME=user -e OC_PASSWD=password --name engineering-bay 42school/engineering-bay
